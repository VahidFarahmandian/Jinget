name:  .NET

on:
  push:
    branches: [ main ]
env:
  GITHUB_TOKEN: ${{ secrets.Nuget_GitHub_Api_Key }}
  
jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Set Version Number
      run: echo "::set-output name=date::$(date +'%Y.%m.%d').${{github.run_number}}"
